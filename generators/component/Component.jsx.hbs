import React, { PureComponent } from 'react'
import {
	string,
	number,
	bool,
	array,
	func,
	node,
} from 'prop-types'
import noop from 'noop3'
import bem from 'bem-cn'

import './{{ properCase name }}.css'

const b = bem('test')


export default class {{ properCase name }} extends PureComponent {
	static propTypes = {

	}

	static defaultProps = {

	}

	render() {
		// const {  } = this.props

		return (
			<div className={cx('{{ camelCase name }}')}>
				{{ properCase name }}
			</div>
		)
	}
}
